ditonton berapa kali(diklik berapa kali?)
bagian warta:
-jumlah minggu
-jumlah warta
-warta terbaru
-list warta:
 -table:
  -nama
  -gambar
  -url
  -url2
  -judul/title
  -terbaru? (bool(true/false))

 -fitur:
  -titik 3:
   -add
   -edit
   -delete

tahap 1: pass; // (GUP_HARVEST)
tahap 2: jumlah minggu,
	 jumlah warta,
	 warta terbaru;
tahap 3: list warta;

storage:
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}




rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.aunt == null;
    }
  }
}



service firebase.storage {
  match /b/{bucket}/o {
    match /images/{allPaths=**} {
      allow read, write;
    }
  }
}
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
